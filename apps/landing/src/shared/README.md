# shared

레이어 최하단. 도메인 지식이 없는 앱 내부 공용 코드.

- `ui/` : 이 앱 전용 UI (모노레포 전역 공유는 `@gdg/ui` 패키지를 쓴다)
- `lib/` : 순수 유틸·헬퍼·훅
- `api/` : 이 앱 전용 API 호출 (공유 API 는 `@gdg/api` 패키지로 분리 예정)

shared 는 다른 레이어를 import 하지 않는다.
