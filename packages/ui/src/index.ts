// @gdg/ui — 모노레포 전역 공유 디자인 시스템.
// 도메인(랜딩/온보딩/스터디) 무관한 순수 UI만 둔다.
// 시각적 복잡도에 따라 atoms / molecules 로 나눈다 (아토믹 디자인 차용).

export { Button } from './atoms/Button'
export type { ButtonProps } from './atoms/Button'
